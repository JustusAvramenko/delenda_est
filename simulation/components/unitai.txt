Did this to make captured horses controllable.
https://wildfiregames.com/forum/index.php?/topic/25948-capturable-animals/&do=findComment&comment=374893

Lines 195, 267, 295, 321 : remove "this.IsAnimal() && !this.IsDomestic() || "
Remove lines 246 to 252
		// We have no control over non-domestic animals.
		if (this.IsAnimal() && !this.IsDomestic())
		{
			this.FinishOrder();
			return;
		}
